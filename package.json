{
  "name": "sih26171-browser-agent",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter extension dev",
    "build": "pnpm -r build",
    "server": "python server/receiver.py",
    "schema:sync": "bash scripts/sync-schemas.sh",
    "eval": "pnpm --filter evaluation exec tsx src/report.ts",
    "test": "pnpm --filter evaluation exec vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@11.13.1"
}
