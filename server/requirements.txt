# V.A.R.M.A receiver server
#
# The agent talks to vLLM over the OpenAI-compatible HTTP API and drives the
# browser over the Chrome DevTools Protocol. browser-use is intentionally NOT a
# dependency: the agent loop is purpose-built for low latency (see server/agent/).

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
playwright>=1.48.0
httpx>=0.27.0

# After installing, fetch the Chromium build Playwright drives:
#   python -m playwright install chromium
